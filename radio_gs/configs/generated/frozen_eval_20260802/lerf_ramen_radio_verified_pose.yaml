base_config: ../query_consistency/lerf_ramen_radio_verified_pose.yaml

# Execution-only mount relocation.  Camera identities and poses remain the
# exact COLMAP records bound by the frozen render authority.
scene_root: /mnt/pool/sqy/3d_understanding/lerf_ovs/ramen
