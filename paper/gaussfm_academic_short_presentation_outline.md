# GaussFM Academic Short Presentation Outline

- PPTX: `/root/RADIO-GS/paper/gaussfm_academic_short_presentation.pptx`
- Format: 11-slide concise academic talk deck
- Narrative: problem -> method -> evidence -> takeaway

## 1. GaussFM: Compact Foundation-Feature Gaussian Memory
- Open with one sentence: GaussFM is a deployable 3D foundation-feature memory, not a task-specific classifier.

## 2. Why 2D foundation features are not yet 3D scene memory
- Motivation slide: identify the representation gap rather than criticizing prior work broadly.

## 3. Research question
- This slide creates the slots that the method components will fill.

## 4. Method overview: one compact memory, three query modes
- Use the framework figure as the visual anchor. Speak in train/inference terms, not module-by-module detail.

## 5. Core components in the compact feature field
- Method slide stays concise: four components, each tied to a bottleneck.

## 6. Main contributions and innovations
- This is the explicit innovation slide requested by the user.

## 7. Result 1: rendered-view open-vocabulary grounding
- Evidence 1: show that the memory renders useful dense maps.

## 8. Result 2: direct 3D open-vocabulary object selection
- Evidence 2: direct 3D query validates the stored scene memory.

## 9. Result 3: VALA-aligned ScanNet point query
- Evidence 3: ScanNet establishes cross-dataset point-query transfer under a VALA-aligned protocol.

## 10. Why it works: compact reconstruction plus support calibration
- This slide makes the method credible without flooding the audience with all ablation rows.

## 11. Takeaway
- Close by returning to the central thesis and the three evidence tracks.
